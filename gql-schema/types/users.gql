type Wallet {
  address: String!
  ens: String
  tokens: [Token!]!
}

type User {
  id: ID!
  walletAddress: String! @deprecated(reason: "wallet object has required address parameter")
  wallet: Wallet!
  followedDaos: [DAO!]!
  avatarUrl: String!
}
