type Proposal {
  id: ID!
  snapshotId: ID!
  title: String!
  dao: DAO!
  juniorDescription: String!
  middleDescription: String!
  seniorDescription: String!
  # UNIX Timestamp (in ms)
  startAt: String!
  # UNIX Timestamp (in ms)
  endAt: String!
  author: String!
}
