type Query {
  me: User!
  proposals(daoIds: [ID!], onlyFollowedDaos: Boolean): [Proposal!]!
  daos(ids: [ID!], onlyFollowed: Boolean): [DAO!]!
}

type Mutation {
  registerUser(walletAddress: ID!): User!

  followDao(daoId: ID!): DAO!
  unfollowDao(daoId: ID!): DAO!
}
