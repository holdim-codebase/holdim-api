type Query {
  me: User!
  proposals: [Proposal!]!
  daos(ids: [ID!]): [DAO!]!
}

type Mutation {
  registerUser(walletAddress: ID!): User!

  followDao(daoId: ID!): DAO!
  unfollowDao(daoId: ID!): DAO!
}
